export const metadata = {
  title: "Sign Up With Email - Lumina",
  description: "Create your Lumina account with email",
}

export default function SignupEmailLayout({ children }) {
  return children
}