export const metadata = {
  title: "Reset Password — Lumina",
  description: "Create a new password for your Lumina account",
}

export default function ResetPasswordLayout({ children }) {
  return children
}